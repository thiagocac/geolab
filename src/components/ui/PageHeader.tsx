export function PageHeader({ kicker, title, description }: { kicker?: string; title: string; description?: string }) { return <header className="mb-6"><p className="kicker">{kicker}</p><h1 className="mt-1 text-3xl display text-slate-950 dark:text-slate-50">{title}</h1>{description ? <p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600 dark:text-slate-300">{description}</p> : null}</header>; }
