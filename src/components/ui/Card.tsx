import type { HTMLAttributes, ReactNode } from 'react';
export function Card({ className='', ...props }: HTMLAttributes<HTMLDivElement>) { return <div className={`card ${className}`} {...props} />; }
export function CardHeader({ kicker, title, children }: { kicker?: string; title: string; children?: ReactNode }) { return <div className="border-b border-slate-100 dark:border-slate-800 p-5"><p className="kicker">{kicker}</p><h2 className="mt-1 text-xl font-black text-slate-950 dark:text-slate-50">{title}</h2>{children ? <p className="mt-1 text-sm text-slate-600 dark:text-slate-300">{children}</p> : null}</div>; }
