// cron-nc-digest (Concresoft) - digest diario de NCs abertas nas ultimas 24h por tenant -> admins/gestores
// via send-notification (x-notify-secret). Fail-closed CRON_SECRET. Clone do cron-digest (query = non_conformities).
// Corpo deployado via MCP (slug cron-nc-digest, sha 51e398f8...). Ver banco vivo para o corpo completo.
